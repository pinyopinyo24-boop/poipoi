# PoiPoi Portable - 自宅PC版ハイブリッド構成ガイド

## 概要

PoiPoi Portable は、自宅 PC で実行できるスタンドアロン版です。ローカルサーバーとクラウドサーバーのハイブリッド構成により、以下が実現できます:

- **自宅 PC 起動時**: ローカルサーバーで高速処理
- **自宅 PC 停止時**: クラウドサーバーへ自動切替
- **スマートフォン**: PC 起動時は LAN 接続、停止時はクラウド接続
- **複数端末**: データ自動同期

---

## 必要環境

### 最小要件

| 項目 | 要件 |
|------|------|
| OS | Windows 10/11, macOS 10.15+, Linux |
| Node.js | v18.0.0 以上 |
| npm | v9.0.0 以上 |
| メモリ | 4GB 以上 |
| ディスク | 2GB 以上の空き容量 |
| ネットワーク | インターネット接続 |

### 推奨環境

| 項目 | 推奨 |
|------|------|
| OS | Windows 11, macOS 13+, Ubuntu 22.04+ |
| Node.js | v20.0.0 以上 |
| メモリ | 8GB 以上 |
| ディスク | 5GB 以上の空き容量 |
| ネットワーク | 有線 LAN (安定性向上) |

### インストール

#### 1. Node.js のインストール

[https://nodejs.org/](https://nodejs.org/) から LTS 版をダウンロードしてインストールしてください。

インストール確認:
```bash
node --version
npm --version
```

#### 2. PoiPoi Portable のダウンロード

GitHub リポジトリからダウンロード:
```bash
git clone https://github.com/pinyopinyo24-boop/poipoi.git
cd poipoi
```

または ZIP ファイルを解凍してください。

---

## 初回設定

### 1. API キーの設定

#### Windows

1. `scripts/start.bat` をダブルクリック
2. 初回起動時に `.env` ファイルが自動作成されます
3. `.env` ファイルを開いて API キーを設定:

```bash
# .env ファイル
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...
CLAUDE_API_KEY=sk-ant-...
```

#### macOS / Linux

```bash
# 1. 依存関係をインストール
npm install

# 2. .env ファイルを作成
cp .env.example .env

# 3. .env を編集して API キーを設定
nano .env

# 4. サーバーを起動
npm run dev
```

### 2. API キーの取得

#### OpenAI API

1. [https://platform.openai.com/](https://platform.openai.com/) にアクセス
2. アカウントを作成/ログイン
3. API キーを生成
4. `.env` に貼り付け

#### Google Gemini API

1. [https://makersuite.google.com/app/apikey](https://makersuite.google.com/app/apikey) にアクセス
2. 「API キーを作成」をクリック
3. キーをコピーして `.env` に貼り付け

#### Anthropic Claude API

1. [https://console.anthropic.com/](https://console.anthropic.com/) にアクセス
2. API キーを生成
3. `.env` に貼り付け

### 3. 設定確認

```bash
npm run check
```

出力例:
```
✓ Node.js v20.0.0
✓ npm v9.0.0
✓ OpenAI API: 設定済み
✓ Gemini API: 設定済み
⚠ Claude API: 未設定 (オプション)
```

---

## 起動方法

### Windows

#### 方法 1: スクリプト使用 (推奨)

1. `scripts/start.bat` をダブルクリック
2. コマンドプロンプトが開き、サーバーが起動します
3. ブラウザで `http://localhost:3000` にアクセス

#### 方法 2: コマンドラインから

```bash
npm run dev
```

### macOS / Linux

```bash
npm run dev
```

### サーバー起動確認

ブラウザで以下にアクセス:
```
http://localhost:3000
```

ログイン画面が表示されれば成功です。

---

## スマートフォン接続方法

### 自宅 PC 上のサーバーに接続

#### 1. PC の IP アドレスを確認

**Windows**:
```bash
ipconfig
```

出力例:
```
IPv4 アドレス: 192.168.1.100
```

**macOS / Linux**:
```bash
ifconfig
```

#### 2. スマートフォンで接続

1. スマートフォンを同じ WiFi に接続
2. ブラウザで以下にアクセス:
   ```
   http://192.168.1.100:3000
   ```

3. ログイン画面が表示されます

#### 3. 接続状態確認

PoiPoi アプリの設定画面で:
- 🏠 **Local** と表示 → PC 接続中
- ☁️ **Cloud** と表示 → クラウド接続中

---

## クラウド切替方法

### 自動切替 (推奨)

1. PoiPoi アプリの設定画面を開く
2. 「接続設定」タブで「自動」を選択
3. PC 起動時: 自動的にローカル接続
4. PC 停止時: 自動的にクラウド接続

### 手動切替

1. 設定画面の「接続設定」を開く
2. 接続モードを選択:
   - **自動**: PC 起動時はローカル、停止時はクラウド
   - **ローカルのみ**: 常に PC サーバーを使用
   - **クラウドのみ**: 常にクラウドサーバーを使用

### 接続状態表示

| 表示 | 状態 | 説明 |
|------|------|------|
| 🟢 Local | ローカル接続 | PC サーバーに接続中 |
| 🔵 Cloud | クラウド接続 | クラウドサーバーに接続中 |
| 🔴 Offline | オフライン | サーバーに接続できません |

---

## データ同期

### 同期対象

以下のデータが自動的に同期されます:

- **会話履歴**: チャット内容
- **メモリ**: AI 学習データ
- **設定**: ユーザー設定
- **生産データ**: 分析結果
- **学習情報**: AI 改善提案

### 同期設定

1. 設定画面の「同期設定」を開く
2. 以下を設定:
   - **自動同期**: ON/OFF
   - **同期間隔**: 30秒/1分/5分/10分
   - **競合解決**: ローカル優先/クラウド優先/マージ/手動

### 同期状態確認

設定画面で以下が表示されます:

- **待機中**: 同期待ちのデータ数
- **同期済み**: 同期完了したデータ数
- **失敗**: 同期に失敗したデータ数
- **競合**: 競合が発生したデータ数

---

## トラブルシューティング

### サーバーが起動しない

**症状**: `npm run dev` で エラーが出る

**対処法**:
```bash
# 1. 依存関係を再インストール
npm install

# 2. キャッシュをクリア
npm cache clean --force

# 3. ポート 3000 が使用中でないか確認
netstat -an | grep 3000  # macOS/Linux
netstat -ano | findstr :3000  # Windows

# 4. ポート 3000 を使用しているプロセスを停止
# Windows: taskkill /PID <PID> /F
# macOS/Linux: kill -9 <PID>
```

### スマートフォンから接続できない

**症状**: `http://192.168.1.100:3000` にアクセスできない

**対処法**:
1. PC とスマートフォンが同じ WiFi に接続しているか確認
2. ファイアウォールで ポート 3000 を許可
3. PC の IP アドレスを再確認
4. ルーターを再起動

### API キーエラー

**症状**: "API key not found" エラー

**対処法**:
1. `.env` ファイルが存在するか確認
2. API キーが正しく設定されているか確認
3. API キーに余分なスペースがないか確認
4. 設定画面で「設定を保存」をクリック

### データが同期されない

**症状**: PC とスマートフォンのデータが異なる

**対処法**:
1. 設定画面で「今すぐ同期」をクリック
2. 同期状態を確認
3. ネットワーク接続を確認
4. サーバーログを確認: `npm run logs`

---

## セキュリティ

### API キー保護

- API キーは `.env` に保存され、暗号化されます
- `.env` ファイルを他人に共有しないでください
- `.gitignore` に `.env` が含まれています

### ローカルネットワーク

- ローカルサーバーは同じ WiFi 内でのみアクセス可能
- インターネット経由でのアクセスはできません
- 必要に応じて VPN を使用してください

### クラウドサーバー

- クラウドサーバーは HTTPS で保護されています
- ログイン情報は暗号化されます
- 定期的にバックアップが実行されます

---

## よくある質問

### Q: PC を起動したままにする必要がある?

**A**: いいえ。PC を停止すると自動的にクラウドサーバーに切り替わります。

### Q: スマートフォンだけで使用できる?

**A**: はい。ただし、ローカルサーバーの高速処理は利用できません。

### Q: 複数の PC で実行できる?

**A**: はい。各 PC で独立して実行できます。ただし、同じクラウドアカウントを使用する場合、データが共有されます。

### Q: オフラインで使用できる?

**A**: ローカルサーバーモードではオフライン使用可能です。ただし、AI 機能は API キーが必要です。

### Q: バックアップはどうする?

**A**: `data/` ディレクトリを定期的にバックアップしてください。

---

## サポート

問題が発生した場合:

1. ログを確認: `npm run logs`
2. GitHub Issues で報告: [Issues](https://github.com/pinyopinyo24-boop/poipoi/issues)
3. ドキュメントを確認: [Wiki](https://github.com/pinyopinyo24-boop/poipoi/wiki)

---

## ライセンス

MIT License - 詳細は LICENSE ファイルを参照

---

**最終更新**: 2026-07-22
